name: kostcheck
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_launcher_icons: ^0.13.1
  shared_preferences: ^2.2.2

flutter_icons:
  android: true
  ios: false
  image_path: img/logo_app_koscheck.png

flutter:
  uses-material-design: true
  assets:
    - img/